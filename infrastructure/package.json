{
  "name": "@ecobeat/infrastructure",
  "version": "1.0.0",
  "description": "AWS CDK infrastructure for Ecobeat",
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy": "npm run build && cdk deploy",
    "destroy": "cdk destroy",
    "diff": "npm run build && cdk diff",
    "synth": "npm run build && cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.160.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3",
    "aws-cdk": "^2.160.0"
  }
}
