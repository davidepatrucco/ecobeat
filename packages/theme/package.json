{
  "name": "@ecobeat/theme",
  "version": "1.0.0",
  "private": true,
  "description": "Design tokens and theme configuration for Ecobeat",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
